import {useState} from "react";import {useNavigate} from "react-router-dom";import {ShieldCheck,Eye,EyeOff,Sparkles} from "lucide-react";import {login,getCurrentUser} from "../api/auth";
export default function Login(){const navigate=useNavigate();const [email,setEmail]=useState("developer@wings.ai"),[password,setPassword]=useState(""),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState("");
async function submit(e){e.preventDefault();setLoading(true);setError("");try{const data=await login(email,password);const token=data.access_token||data.token;if(!token)throw new Error("Login response did not contain an access token.");localStorage.setItem("wings_access_token",token);try{const user=await getCurrentUser();localStorage.setItem("wings_user",JSON.stringify(user))}catch{localStorage.setItem("wings_user",JSON.stringify({email}))}navigate("/dashboard",{replace:true})}catch(err){setError(err.response?.data?.detail||err.message||"Unable to sign in.")}finally{setLoading(false)}}return <div className="login-page"><div className="login-glow glow-a"/><div className="login-glow glow-b"/><div className="login-card"><div className="login-brand"><div className="login-logo"><ShieldCheck size={30}/></div><span>WINGS AI</span></div><div className="login-heading"><div className="eyebrow"><Sparkles size={13}/> AI-NATIVE IT OPERATIONS</div><h1>Welcome back</h1><p>Sign in to your enterprise operations workspace.</p></div>{error&&<div className="error-box">{error}</div>}<form onSubmit={submit} className="form-stack"><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<div className="password-wrap"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?"text":"password"} required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><button className="primary-btn login-btn" disabled={loading}>{loading?"Signing in...":"Sign in to WINGS"}</button></form><div className="login-footer"><span>Secure enterprise access</span><span>JWT protected</span></div></div></div>}